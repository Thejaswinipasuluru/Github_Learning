name: test-WF
on:
  push:
    branches:
      - main
      - feature/test-demo
  pull_request:
    branches:
      - main
      - develop
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: Build App
        run: echo "Building an application"
  Test:
    runs-on: ubuntu-latest
    needs: Build
    steps:
      - name: Test App
        run: echo "Testing an application"
  Deploy:
    runs-on: ubuntu-latest
    needs: Test
    steps:
      - name: Deploy App
        run: echo "Deploying an application"

      
