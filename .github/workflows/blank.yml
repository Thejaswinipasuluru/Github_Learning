name: first-WF
on:
  push:
    branches:
      - main
      - develop
      - release/**
  pull_request:
    branches:
      - main
jobs:
  Build:
    runs-on: ubuntu-latest
    steps:
      - name: build-step
        run: echo "A demo build"
  
      
